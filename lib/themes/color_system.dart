import 'package:flutter/material.dart';

ThemeData nature = ThemeData(
  colorScheme: ColorScheme.light(
      primary: Color(0xff273d24), // 대표 색상
      onPrimary: Color(0xff000f0e), // 글자 색상
      background: Color(0xFFF5F5F5),
      secondary: Color(0xffd2dbc8), // 연한 색상
      tertiary: Color(0xffe9facd), // 강조 색상
  ),
);